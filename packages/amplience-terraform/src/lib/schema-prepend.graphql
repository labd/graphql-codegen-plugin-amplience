enum ValidationLevel {
  SLOT
  CONTENT_TYPE
}
type Visualization {
  label: String!
  templated_uri: String!
  default: Boolean
}
directive @amplience(
  repository: String
  validationLevel: ValidationLevel
  visualizations: [Visualization!]
) on OBJECT
