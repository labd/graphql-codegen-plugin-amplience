type Test @amplience {
  name: String
}

type TestOtherRepository @amplience(repository: "website2") {
  name: String
}

type TestSlot @amplience(validationLevel: SLOT) {
  name: String
}

type TestVisualizations
  @amplience(
    visualizations: true
    # visualizations: [
    #   {
    #     label: "Production"
    #     templated_uri: "https://example.com/preview/with-layout?vse={{vse.domain}}&content={{content.sys.id}}"
    #     default: true
    #   }
    #   {
    #     label: "Acceptance"
    #     templated_uri: "https://acc.example.com/preview/with-layout?vse={{vse.domain}}&content={{content.sys.id}}"
    #     default: false
    #   }
    # ]
  ) {
  name: String
}

type NotATest {
  name: String!
}
