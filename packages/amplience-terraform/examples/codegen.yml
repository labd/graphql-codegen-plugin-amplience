overwrite: true
hooks:
  afterAllFileWrite:
    - terraform fmt
generates:
  examples/output/bla.tf:
    schema: ./examples/schema.graphql
    plugins:
      - ./dist/index.js
    config:
      hostname: 'https://schema-examples.com'
